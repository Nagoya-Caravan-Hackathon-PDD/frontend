import { LoginButton } from './LoginButton';
import { Center } from '@/shared/components/layouts/Center';

export const Login = () => {
  return (
    <Center>
      <LoginButton />
    </Center>
  );
};
